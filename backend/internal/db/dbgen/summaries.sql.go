// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: summaries.sql

package dbgen

import (
	"context"

	uuid "github.com/google/uuid"
	stdtime "time"
)

const getMonthlySummary = `-- name: GetMonthlySummary :one
SELECT id, household_id, month, summary, model, created_at FROM monthly_summaries
WHERE household_id = $1 AND month = $2
`

type GetMonthlySummaryParams struct {
	HouseholdID uuid.UUID    `json:"household_id"`
	Month       stdtime.Time `json:"month"`
}

func (q *Queries) GetMonthlySummary(ctx context.Context, arg GetMonthlySummaryParams) (MonthlySummary, error) {
	row := q.db.QueryRow(ctx, getMonthlySummary, arg.HouseholdID, arg.Month)
	var i MonthlySummary
	err := row.Scan(
		&i.ID,
		&i.HouseholdID,
		&i.Month,
		&i.Summary,
		&i.Model,
		&i.CreatedAt,
	)
	return i, err
}

const upsertMonthlySummary = `-- name: UpsertMonthlySummary :one
INSERT INTO monthly_summaries (household_id, month, summary, model)
VALUES ($1, $2, $3, $4)
ON CONFLICT (household_id, month) DO UPDATE SET
    summary    = EXCLUDED.summary,
    model      = EXCLUDED.model,
    created_at = now()
RETURNING id, household_id, month, summary, model, created_at
`

type UpsertMonthlySummaryParams struct {
	HouseholdID uuid.UUID    `json:"household_id"`
	Month       stdtime.Time `json:"month"`
	Summary     string       `json:"summary"`
	Model       string       `json:"model"`
}

// Regenerating overwrites the cached text and stamps a fresh created_at.
func (q *Queries) UpsertMonthlySummary(ctx context.Context, arg UpsertMonthlySummaryParams) (MonthlySummary, error) {
	row := q.db.QueryRow(ctx, upsertMonthlySummary,
		arg.HouseholdID,
		arg.Month,
		arg.Summary,
		arg.Model,
	)
	var i MonthlySummary
	err := row.Scan(
		&i.ID,
		&i.HouseholdID,
		&i.Month,
		&i.Summary,
		&i.Model,
		&i.CreatedAt,
	)
	return i, err
}
